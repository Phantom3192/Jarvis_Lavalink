server:
  port: 2333
  address: 0.0.0.0

lavalink:
  server:
    password: "jarvisbot"
    sources:
      youtube: false      # disabled — let public node handle it
      soundcloud: true    # ← this is all you need
      http: true

plugins: []   # no plugins needed for SC
